#include <fcntl.h>
#include <unistd.h>

int main() {
    int f1 = open("input.txt", O_RDONLY);
    int f2 = open("output.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    char ch;

    while (read(f1, &ch, 1) > 0)
        write(f2, &ch, 1);

    close(f1);
    close(f2);
    return 0;
}
